# 错误记录汇总 —— 工程 9(SPI Touch) & 10(LVGL Display)

> 用途:回顾从工程 9 到工程 10 编译期间遇到的**致命/重要错误**。每个错误包含:错误信息、根本原因、解决方法。
> 当前状态:**10.LVGL Display 已编译通过**,产物 `build/10.LVGL.bin`(1.26MB)。

---

## 工程 9:SPI Touch

### 错误 1:`esp_lcd_touch_xpt2046.h: No such file or directory`

```
fatal error: esp_lcd_touch_xpt2046.h: No such file or directory
```

**原因**:`touch_set` 组件 `#include "esp_lcd_touch_xpt2046.h"`,但这个头文件属于组件 `atanisoft__esp_lcd_touch_xpt2046`,而 `touch_set/CMakeLists.txt` 里**没有声明对这个组件的依赖**。

**背景知识(核心)**:ESP-IDF 的 `idf_component_register()` 里的 `REQUIRES` / `PRIV_REQUIRES` 决定:编译本组件时,哪些组件的 include 目录会被加进编译命令。没声明依赖 → 编译器找不到头文件。

**解决**:在 `touch_set/CMakeLists.txt` 加上 `PRIV_REQUIRES atanisoft__esp_lcd_touch_xpt2046`。

---

### 错误 2:`TOUCH_CLK_PIN` undeclared / `touch_dev_t` unknown type

```
error: 'TOUCH_CLK_PIN' undeclared (first use in this function)
error: unknown type name 'touch_dev_t'
```

**原因**:编译时 `touch_set.h` 还只是一个 **18 字节的占位文件**(内容只有 `void func(void);`),引脚宏定义和结构体都还没写进去。**编译发生在文件填充内容之前** —— 构建时间 vs 文件修改时间的错位(用文件时间戳证明了"文件在编译之后才写完")。

**教训**:头文件改完再编译;看到"未定义"先确认文件内容是否真的写好了,别急着改代码。

---

## 工程 10:LVGL Display

### 错误 3:`idf.py fullclean` 拒绝执行 —— managed_components 校验失败

```
ERROR: Some components in the managed_components directory were modified.
```

**原因**:`managed_components/` 是构建时自动下载的第三方组件,ESP-IDF 用 `CHECKSUMS.json` 校验文件哈希。从工程 9 复制工程时,**带过来两个被改动过的文件**:
- `esp_lcd_touch_xpt2046.c`(14829 vs 标准 14414 字节)
- `espressif` 的 CMakeLists.txt(REQUIRES 顺序被调换 + CRLF 行尾)

所以 `idf.py fullclean` 拒绝删除(防止误删用户改动)。

**解决**:确认是复制带来的无意义改动 → **丢弃改动,删掉 managed_components 目录**,下次构建自动重新下载。

---

### 错误 4:`'lcd_dev_t' has no member named 'panel_io'`

```
error: 'lcd_dev_t' has no member named 'panel_io'; did you mean 'panel'?
```

**原因**:第 10 章是第 9 章的**升级版**,`lcd_dev_t` 结构体需要 `panel` 和 `panel_io` **两个成员**(第 10 章 LVGL 适配器注册显示设备时要同时传两者)。而复制的第 9 章代码结构体里只有 `panel`。

**教训**:跨章节复制代码时,注意章节间 API 的升级/变更,结构体、函数签名都可能变。

---

### 错误 5:`touch_init` 参数个数不匹配 + `lvgl_init` 隐式声明

```
error: too many arguments to function 'touch_init'
error: implicit declaration of function 'lvgl_init'
```

**原因**:**第 10 章改了 touch 的 API**,而 `touch_set` 还是第 9 章的旧接口:
- 第 9 章:`void touch_init(void);`
- 第 10 章:`void touch_init(touch_dev_t *dev);`(要传设备指针,函数内部把句柄存进 `dev->handle`)
- 同时 `touch_dev_t` 结构体新增成员 `esp_lcd_touch_handle_t handle;`
- `lvgl_set` 组件当时还是空壳(stub),没有 `lvgl_init` 的实现

**解决**:按第 10 章 API 迁移 —— 改 `touch_set.h/.c` 的签名、给结构体加 `handle` 成员、补全 `lvgl_set` 的实现。

---

### 错误 6:`esp_lcd_touch_xpt2046.h: No such file or directory`(lvgl_set 编译)

```
Compilation failed because touch_set.h (in "touch_set" component) includes
esp_lcd_touch_xpt2046.h, provided by atanisoft__esp_lcd_touch_xpt2046 component(s).
However, atanisoft__esp_lcd_touch_xpt2046 component(s) is in the private
requirements list of "touch_set".

To fix this, move atanisoft__esp_lcd_touch_xpt2046 from PRIV_REQUIRES into REQUIRES
```

**原因**:**公共头文件** `touch_set.h`(`INCLUDE_DIRS "include"` 里的头)include 了 XPT2046 的头文件。公共头文件里被 include 的组件依赖**必须放 `REQUIRES`**,`PRIV_REQUIRES` 只服务于私有 `.c` 文件。

> ⚠️ 注意:这是和错误 1 **同一个机制**、但**不同级别**:
> - 工程 9:头文件只在 `.c` 里被 include → `PRIV_REQUIRES` 够用
> - 工程 10:头文件在公共 `.h` 里被 include → 必须 `REQUIRES`

**解决**:把 `atanisoft__esp_lcd_touch_xpt2046` 从 `PRIV_REQUIRES` 移到 `REQUIRES`。

**核心知识点**:一个组件 include 了另一个组件的头文件,就**必须声明依赖**。头文件越"公共",依赖级别要求越高:`.c` 私有 → `PRIV_REQUIRES`;`include/` 公共头 → `REQUIRES`。

---

### 错误 7:`app partition is too small` —— 分区表装不下固件

```
Error: app partition is too small for binary 9.SPI.bin size 0x133770:
  - Part 'factory' 0/0 @ 0x10000 size 0x100000 (overflow 0x33770)
```

**数字拆解**:
- 固件 `0x133770` ≈ **1.26 MB**(LVGL 音乐 demo 很大)
- 默认分区 `factory` = `0x100000` = **1 MB**
- 超出 `0x33770` ≈ **206 KB**

**原因**:教程要求用自定义分区表(`sdkconfig.defaults` 已写 `CONFIG_PARTITION_TABLE_CUSTOM=y` + `partitions.csv`,app 分区扩到 2MB)。但**工程是从 9 复制的,旧 `sdkconfig` 一起被带过来**(里面是默认 `SINGLE_APP`,1MB)。关键机制:**`sdkconfig` 文件一旦存在,`sdkconfig.defaults` 就不再生效** → 自定义分区配置从未生效。当时 `partitions.csv` 还是空文件。

**解决**:
1. 填 `partitions.csv`(教程标准:app 分区 `0x200000` = 2MB)
2. 改 `sdkconfig`:分区表从 `SINGLE_APP` 切成 `CUSTOM`(只动这两行,PSRAM、16MB flash 等其余配置保留)

**教训**:复制工程后,如果 `sdkconfig.defaults` 与旧 `sdkconfig` 冲突,**删掉旧 `sdkconfig` 让它重新生成**(或手动同步)。固件超过 1MB 后就要用自定义分区表。

---

## 工程 11:LVGL UI

### 错误 9:build 目录残缺 → `Cannot find source file: main/10.LVGL Display.c`

```
CMake Error: Cannot find source file:
  C:/.../11.LVGL UI/main/10.LVGL Display.c
```

**诡异点**:所有文件明明都改成了 `11.LVGL UI.c`,`main/CMakeLists.txt` 写的也是 `SRCS "11.LVGL UI.c"`,全工程没有任何 `10.LVGL Display` 残留 —— 但 CMake 偏偏找旧文件名。

**真相**:`build/` 目录是**残缺的空壳**(复制工程带过来,又没被完整清理),CMake 读到了里面残留的旧状态。随后 `fullclean` 报 `Directory ... doesn't seem to be a CMake build directory. Refusing to automatically delete` —— 证实 build/ 里连 `CMakeCache.txt` 都没有,不是有效 CMake 目录,`fullclean` 拒绝自动删。

**解决**:手动删:
```cmd
rd /s /q build
idf.py build
```

**教训**:`fullclean` 输出 `Done` 不等于 build 清干净了;残缺 build 目录会让 CMake 读到旧配置报出"指鹿为马"的错误(找不存在的旧文件)。

### 错误 10:`'lv_font_montserrat_12' undeclared`(LVGL 在编译 music demo 源码)

```
error: 'lv_font_montserrat_12' undeclared (first use in this function);
       did you mean 'lv_font_montserrat_16'?
 131 |     font_small = &lv_font_montserrat_12;
```

**注意编译的是谁**:`managed_components/lvgl__lvgl/demos/music/lv_demo_music_main.c` —— **LVGL 自带的 music demo 源码**,不是你的代码。工程 11 是 SquareLine UI,根本不需要它。

**因果链**:
1. `sdkconfig` 第 2695 行残留 `CONFIG_LV_USE_DEMO_MUSIC=y`(从工程 10 复制)
2. LVGL 组件因这个开关把 `demos/music/` 全部编译进工程
3. demo 源码引用 `lv_font_montserrat_12`(小号字体)
4. sdkconfig 里 12 号字体没启用(`# CONFIG_LV_FONT_MONTSERRAT_12 is not set`)→ LVGL 不生成该符号 → 编译错误

**根源**:还是**复制工程带 sdkconfig,defaults 不生效**的老坑 —— 第 3 次变体(工程 10 是分区表 SINGLE_APP,这次是 demo 开关)。

**解决**:手动改 `sdkconfig`,关掉 `CONFIG_LV_USE_DEMO_MUSIC`(及其 landscape 子选项),与 `sdkconfig.defaults`(教程第 11 章没有 demo 配置)保持一致。

**教训**:main 改了、defaults 改了,但 `sdkconfig` 还在用旧的 —— 检查残留配置时 `grep` 一下 `sdkconfig` 本体,别只看 `sdkconfig.defaults`。

### 错误 11:`lvgl.h: No such file or directory`(ui_set 组件)

```
fatal error: lvgl.h: No such file or directory
    4 | #include "lvgl.h"
```

**表面原因**:`ui_set.h` include `lvgl.h`,但组件没声明对 `lvgl__lvgl` 的依赖。

**深层原因**(更值得记):`create-component` 创建的 `ui_set` 只有 `SRCS "ui_set.c" + INCLUDE_DIRS "include"`,**SquareLine 导出的 `ui/` 目录(49 个 .c 文件)完全没接进构建** —— 漏了教程的移植步骤 3/4/5。

**修复**(教程 4272-4280 行,移植后的标准 CMakeLists):
```cmake
file(GLOB_RECURSE UI_SOURCES "ui/*.c")

idf_component_register(SRCS ${UI_SOURCES}
                    INCLUDE_DIRS "include" "ui"
                    REQUIRES lvgl__lvgl)

target_compile_options(${COMPONENT_LIB} PRIVATE -Wno-unused-value)
```

**教训**:SquareLine 导出的 `ui/` 不会自动编译,必须用 `GLOB_RECURSE` 收进组件;`INCLUDE_DIRS` 要加 `"ui"` 才能 `#include "ui.h"`;`-Wno-unused-value` 屏蔽 SquareLine 的 `(e);` 占位符警告。

### 错误 12:`'lv_font_montserrat_48' undeclared`(SquareLine UI 用大字体)

```
ui_Main.c:288:48: error: 'lv_font_montserrat_48' undeclared (first use in this function);
       did you mean 'lv_font_montserrat_16'?
  288 |  lv_obj_set_style_text_font(ui_LabelTime1, &lv_font_montserrat_48, ...)
```

**原因**:SquareLine 设计的时间标签用了 **48 号内置字体**,但 `sdkconfig` 里 48 号字体未启用(`# CONFIG_LV_FONT_MONTSERRAT_48 is not set`)→ LVGL 不生成该符号。又是老套路:`sdkconfig.defaults` 写了 `CONFIG_LV_FONT_MONTSERRAT_48=y`,但**旧 sdkconfig 压过 defaults** 没生效。

**修复**:手动改 `sdkconfig` → `CONFIG_LV_FONT_MONTSERRAT_48=y`。

**教训**:教程 4307 行原文就写着这个坑 —— "编译报 `lv_font_montserrat_XX undeclared`,需要在 `sdkconfig` 中启用对应的字体配置"。SquareLine UI 用到哪个字号,就手动在 sdkconfig 打开哪个(先 `grep -rho "lv_font_montserrat_[0-9]*" components/ui_set/ui/` 看 UI 实际用了哪些)。

### 错误 13:按键点按无反应/不灵敏 —— XPT2046 中断模式检测不到"松开"(运行期问题)

**现象**:UI 烧录成功、画面正常,但按键按下去没反应(只能看到按下效果,CLICKED 事件永远不触发)。

**根因**(跨 3 个组件的链路,环环相扣):
1. XPT2046 的 INT 引脚:按下拉低、松开拉高(只变化一次)
2. `touch_set.c` 配 `levels.interrupt = 0` → 驱动配成 **`GPIO_INTR_NEGEDGE`**(只响应"按下"这个下降沿)—— `esp_lcd_touch_xpt2046.c:104`
3. `esp_lvgl_adapter` 的 **IRQ 模式**:只有中断信号量可用时才读 SPI(`esp_lv_adapter_input_touch.c:1126`)
4. 结果:按下 → 读到"按下" ✓;松开 → 没有边沿 → **永远不再读 → LVGL 永远停在 PRESSED**
5. LVGL 的 CLICKED = 按下+松开同一对象 → 永不触发 → 按键"没反应"

**修复**:`touch_set.c` 里 `.int_gpio_num = TOUCH_IRQ_PIN` 改成 **`GPIO_NUM_NC`**(轮询模式)。适配器每个 LVGL 输入周期(`LV_INDEV_DEF_READ_PERIOD`,默认 30ms)直接读一次 SPI,按下/松开都能稳定检测。代价:30ms 一次 SPI 读取(微秒级),可忽略。

**教训**:带 IRQ 的触摸栈要检查"松开"是否也能检测到 —— 只靠边沿中断 + 中断信号量的组合天然只能检测按下。XPT2046 用轮询模式最省心。**工程 10 的 `touch_set.c` 有同样的坑**,之后回去玩音乐 demo 时一并改。

---

## 环境问题(跨工程,与代码无关)

### 错误 8:Git Bash 下 `idf.py` / `export.bat` 被 MSYSTEM 拦截

```
MSys/Mingw is no longer supported. Please follow the getting started guide...
This .bat file is for Windows CMD.EXE shell only.
```

**原因**:Git Bash(MSYS2)运行时**无条件给所有子进程注入 `MSYSTEM=MINGW64`**,而 ESP-IDF 的 `idf.py` / `idf_tools.py` / `export.bat` 都检测这个变量并拒绝执行。`env -u MSYSTEM`、`unset` 全部无效(运行时在 exec 时重新注入)。

**解决**:用 PowerShell 在**原生进程**里真正删掉环境变量,再启动构建:
```
powershell -NoProfile -Command "Remove-Item Env:MSYSTEM -ErrorAction SilentlyContinue; cmd /c <构建脚本.bat>"
```

**教训**:ESP-IDF 是给 Windows CMD/PowerShell 设计的;从 Git Bash 操作会撞上 MSYSTEM 防护。要么用 PowerShell 剥变量,要么直接用 IDF 的专用终端。

---

## 附:一些有意思的旁支

### `9.SPI.bin` 项目名残留
第 10 章工程编译产物叫 `9.SPI.bin`,因为**复制工程时 `build/` 缓存也带过来了**(`CMakeCache.txt` 里 `CMAKE_PROJECT_NAME=9.SPI`)。CMakeLists.txt 后来改成了 `project(10.LVGL Display)`,但 mtime 判断没触发重新配置。**下次构建会自动重新配置** → 产物变成 `10.LVGL.bin`。

> 顺带验证:`project(10.LVGL Display)` 带空格**在 ESP-IDF 下没问题** —— ESP-IDF 重写了 `project()` 函数(`tools/cmake/project.cmake`),只取第一个参数 `10.LVGL`。裸 CMake 下会报错(第二个参数被当成语言),但 ESP-IDF 里安全。

### git 显示几千个文件
`.gitignore` 原来只忽略 `.vscode/` 和 `build/`,`managed_components/`(自动下载的 LVGL/freetype 等,几千个文件)和 `sdkconfig` 没被忽略。已补充:`sdkconfig`、`sdkconfig.old`、`managed_components/`、`dependencies.lock`。现在每个工程只剩 ~30 个纯源码文件会被跟踪。

---

## 核心知识点速查

| 问题 | 一句话答案 |
| :--- | :--- |
| 找不到第三方头文件 | 组件间 include 必须声明依赖;公共头 → `REQUIRES`,私有 `.c` → `PRIV_REQUIRES` |
| 结构体/函数签名不匹配 | 跨章节复制代码,注意 API 升级(touch_init 加参数、lcd_dev_t 加 panel_io) |
| fullclean 拒绝执行 | `managed_components` 被改过(校验失败)→ 丢弃重新下载 |
| 固件太大装不下 | 自定义分区表 `partitions.csv` 扩 app 分区;`sdkconfig` 存在时 defaults 不生效 |
| Git Bash 跑不了 idf.py | MSYSTEM 注入;用 PowerShell `Remove-Item Env:MSYSTEM` 再调 cmd |
| 项目名还是上一章的 | 复制工程带 `build/` 缓存;删 build 或等自动重配置 |
| build 报"找不到"不存在的旧文件 | 残缺 build 目录残留旧 CMake 状态;`rd /s /q build` 手动删 |
| LVGL 在编译 demo 源码还报错 | `sdkconfig` 残留 `LV_USE_DEMO_MUSIC=y`,与 defaults 同步关掉 |
| 触摸按键没反应/不灵敏 | XPT2046 IRQ 模式检测不到松开;`int_gpio_num` 改 `GPIO_NUM_NC` 轮询 |
